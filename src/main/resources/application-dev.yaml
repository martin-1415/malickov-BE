spring:
  liquibase:
    contexts: dev
    change-log: classpath:db/changelog/app-changelog.xml

security:
  JwtValidityMillis: 28800000


server:
  port: 8443
  ssl:
    key-store-type: PKCS12
    key-store: classpath:keystore-dev.p12
    key-store-password: veryStrongAndHiddenPassword
    key-alias: local-ssl
