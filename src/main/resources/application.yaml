spring:
  application:
    name: malickov-be
  datasource:
    url: jdbc:postgresql://localhost:5432/malickov
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/app-changelog.xml
  profiles:
    active: prod
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

security:
  bcrypt:
    strength: 12

logging:
  level:
    root: INFO
  file:
    name: malickov-be.log
    path: ./logs
  logback:
    rollingpolicy:
      max-history: 14
      total-size-cap: 1GB

server:
    port: 8080