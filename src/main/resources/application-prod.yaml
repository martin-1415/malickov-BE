spring:
  liquibase:
    contexts: prod
    change-log: classpath:db/changelog/app-changelog.xml

security:
  JwtValidityMillis: 900000

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false